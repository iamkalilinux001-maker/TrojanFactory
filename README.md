# TrojanFactory
The Trojan Factory embeds evil files inside normal files using an Autoit script. It takes two URLs: one for a normal file (PDF, MP3, etc.) and one for an evil file (backdoor, keylogger). The output is a single EXE that shows the normal file to the user while running the evil file in the background
